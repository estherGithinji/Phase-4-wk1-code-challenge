databases:
  - name: restaurant-api
    databaseName: restaurant-api
    user: restaurant-api

services:
  - type: web
    name: restaurant-api
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: restaurant-api
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false